/**
 * Quran index — the public surface of the feature (story 6-3).
 *
 * `lint:layers` rule 4: siblings inside this feature import each other directly; everything
 * outside comes through here.
 */

export { QuranIndexScreen, type QuranIndexScreenProps } from './QuranIndexScreen';
